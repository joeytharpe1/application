import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  homeBody: {
    maxWidth: "600px",
    marginBottom: "5px",
    marginTop: "10px",
    paddingBottom: "9px",
  },
});
